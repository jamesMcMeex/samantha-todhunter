{% extends "layout.nunjucks" %}

{% block heading %}

<div class="container">
    <div class="row justify-content-center">
    <div class="col-8">
      <h1 class="page-heading text-center">{{ pageTitle }}</h1>
      <p class="text-center mb-4 mb-md-5">{{ pageStrapline }}</p>
    </div>
  </div>
</div>

{% endblock %}
